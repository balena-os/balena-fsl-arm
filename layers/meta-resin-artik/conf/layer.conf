BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "resin-artik"
BBFILE_PATTERN_resin-artik := "^${LAYERDIR}/"
BBFILE_PRIORITY_resin-artik = "1337"
